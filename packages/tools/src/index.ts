/**
 * Domain tools, MCP prompts and the generic API tools, one module per
 * ChurchTools area. Tools only return data; everything around a call happens
 * in the core.
 *
 * @packageDocumentation
 */
export {};
