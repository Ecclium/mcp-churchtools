// Every project in the workspace, checked together by `tsc -b`. Editors also
// start here: for an open file they look for a tsconfig.json that references
// the project owning it.
{
  "files": [],
  "references": [{ "path": "./tsconfig.build.json" }]
}
