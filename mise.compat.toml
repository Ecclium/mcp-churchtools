# Compatibility environment, selected with MISE_ENV=compat: the tools from
# mise.toml with the next Node line instead, so that the code is also checked
# against the version the container images will move to.
[tools]
node = "26.10.0"
